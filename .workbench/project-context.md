# Project Context — trainer

**Project name:** trainer
**Project type:** pwa
**SDLC reference:** /workbench/project-types/pwa/project.json

## Instructions for Claude — read this first in every session

This project is type `pwa`. Before starting any phase work or SDLC-related task:

1. Read `/workbench/project-types/pwa/project.json` to find the correct SDLC phases,
   deliverables, advance criteria, and skills for this project type.
2. Follow ONLY that SDLC. Do not use the factory SDLC
   (`/workspace/documents/factory/sdlc.md`) unless this project type is `factory`.
   The factory SDLC contains factory-specific artifacts (queue.json, pipeline stages)
   that do not apply to other project types.
3. Phase 0 planning skills (kano-model, task-flow, user-flow, event-storming,
   define-bounded-contexts, ubiquitous-language) are shared across all project types
   and are located in `/workspace/documents/factory/`. Using those skills is correct.
   Using the factory SDLC wrapper around them is not.

Generated by `make activate` on 2026-06-16. Re-generated on every activation.
