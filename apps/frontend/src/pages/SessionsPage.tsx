// ------------------------------------------------------------
// SessionsPage.tsx
// Session list and the active workout tracking view.
// This will be the most complex page in the app.
// ------------------------------------------------------------
export default function SessionsPage(): React.JSX.Element {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold mb-4">Sessions</h1>
      <p className="text-gray-400">Session list and workout tracking will appear here.</p>
      {/* Phase 3: session list by client, create session (from template or live),
                  active session view with workout blocks,
                  exercise list per workout, set logging UI (reps/weight/time),
                  quick-add exercise mid-session */}
    </div>
  )
}
