{
  "name": "@trainer-app/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript types, Zod schemas, and utilities used by both frontend and backend",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'shared: no lint needed (tsc covers it)'"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
